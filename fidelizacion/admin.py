from django.contrib import admin  # noqa: F401

# El panel de administración de Django (django.contrib.admin) se habilitará
# en la Unidad 2, junto con los modelos y la base de datos del sistema
# VikingCard. Por eso no está en INSTALLED_APPS todavía (ver vikingcard/settings.py).
