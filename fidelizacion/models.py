from django.db import models  # noqa: F401

# Evaluación 1 (Unidad 1): sin base de datos todavía.
#
# Los "modelos" de este caso (Usuario/Cuenta VikingCard, Movimiento, Nivel,
# Servicio) están representados como diccionarios en `fidelizacion/data.py`,
# cargados desde `fidelizacion/mock_data/*.json`. En la Unidad 2
# ("Framework back end") estas mismas entidades se migrarán a clases
# `models.Model` reales con su respectiva base de datos.
